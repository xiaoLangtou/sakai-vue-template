# 菜单搜索功能使用说明

## 📦 安装依赖

菜单搜索功能需要安装拼音转换库 `pinyin-pro`：

```bash
pnpm add pinyin-pro
```

## ✨ 功能特性

### 1. **智能搜索**
- ✅ **中文匹配**：直接输入中文关键词
- ✅ **拼音首字母**：输入 `yhgl` 可以找到 "用户管理"
- ✅ **完整拼音**：输入 `yonghu` 也能找到 "用户管理"
- ✅ **路径匹配**：支持路由路径搜索
- ✅ **面包屑匹配**：支持完整路径搜索（如：系统管理 > 用户管理）

### 2. **快捷键支持**
- **打开搜索**：`Ctrl + K` (Windows/Linux) 或 `⌘ + K` (Mac)
- **关闭搜索**：`Esc`
- **上下导航**：`↑` / `↓` 方向键
- **选择菜单**：`Enter`

### 3. **用户体验**
- 🎯 实时搜索，无需等待
- 🎨 高亮匹配文本
- ⌨️ 完整键盘导航支持
- 📱 响应式设计，支持移动端
- 🌙 深色模式适配
- 🔝 最多显示 10 条结果

## 🎯 使用场景

### 场景 1：快速定位功能
```
输入：yhgl
结果：用户管理
```

### 场景 2：模糊搜索
```
输入：user
结果：用户管理、用户列表、用户设置...
```

### 场景 3：路径搜索
```
输入：/system/user
结果：用户管理
```

## 🔧 技术实现

### 组件结构
```
src/layout/
├── shared/
│   ├── app-menu-search.vue    # 搜索组件
│   ├── app-menu.vue            # 菜单组件
│   └── index.ts                # 导出
└── default-layout/
    └── app-sidebar.vue         # 侧边栏（集成搜索）
```

### 核心功能

#### 1. 拼音转换
```typescript
import pinyin from 'pinyin-pro';

// 获取首字母
getPinyinInitials('用户管理') // => 'yhgl'

// 获取完整拼音
getFullPinyin('用户管理') // => 'yonghuguanli'
```

#### 2. 菜单扁平化
```typescript
// 将树形菜单转为一维数组，便于搜索
const flatMenuList = flattenMenu(menuList);
```

#### 3. 智能匹配
```typescript
const matchSearch = (menu, keyword) => {
    // 1. 标题匹配
    // 2. 路径匹配
    // 3. 拼音首字母匹配
    // 4. 完整拼音匹配
    // 5. 面包屑路径匹配
};
```

## 🎨 样式定制

搜索框样式可以通过以下方式自定义：

```vue
<!-- 修改搜索按钮样式 -->
<button class="w-full flex items-center gap-2 px-3 py-2 ...">
    <Search :size="16" />
    <span>搜索菜单...</span>
</button>
```

## 📝 注意事项

1. **性能优化**
   - 搜索结果限制为 10 条
   - 使用 `computed` 缓存搜索结果
   - 扁平化菜单只计算一次

2. **安全性**
   - 使用 `v-html` 渲染高亮文本时，输入已经过滤
   - 只匹配菜单标题，不执行任意代码

3. **兼容性**
   - 支持 Vue 3.4+
   - 需要 `pinyin-pro` 库支持
   - 快捷键在所有现代浏览器中可用

## 🚀 扩展功能

### 添加搜索历史
```typescript
const searchHistory = ref<string[]>([]);

const addToHistory = (keyword: string) => {
    if (!searchHistory.value.includes(keyword)) {
        searchHistory.value.unshift(keyword);
        searchHistory.value = searchHistory.value.slice(0, 5);
    }
};
```

### 添加热门搜索
```typescript
const hotSearches = ref([
    '用户管理',
    '角色管理',
    '菜单管理',
    '系统设置'
]);
```

### 添加搜索统计
```typescript
const searchStats = ref<Record<string, number>>({});

const trackSearch = (menuId: string) => {
    searchStats.value[menuId] = (searchStats.value[menuId] || 0) + 1;
};
```

## 🐛 故障排除

### 问题 1：找不到 pinyin-pro 模块
```bash
# 解决方案：安装依赖
pnpm add pinyin-pro
```

### 问题 2：快捷键不生效
```typescript
// 检查是否正确添加了事件监听
onMounted(() => {
    document.addEventListener('keydown', handleKeydown);
});

onUnmounted(() => {
    document.removeEventListener('keydown', handleKeydown);
});
```

### 问题 3：搜索结果不准确
```typescript
// 调整匹配逻辑的优先级
// 1. 精确匹配 > 2. 拼音首字母 > 3. 完整拼音 > 4. 模糊匹配
```

## 📚 相关文档

- [pinyin-pro 文档](https://github.com/zh-lx/pinyin-pro)
- [Vue 3 文档](https://vuejs.org/)
- [Lucide Icons](https://lucide.dev/)
